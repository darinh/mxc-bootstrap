{
  "schemaVersion": "0.6.0-alpha",
  "containment": "process",
  "readScope": "drive",
  "scopedTempLabel": "mxc-scratch",
  "networkDefault": "deny",
  "notes": "These document the defaults baked into policy.mjs. Behavior is normally selected via identity profiles in profiles/*.json; env overrides include MXC_PROFILE, MXC_REPO_ROOT, MXC_FORCE_NO_NETWORK, MXC_SCHEMA_VERSION, and MXC_CONTAINMENT (Windows backend: process=BaseContainer, windows_sandbox=VM)."
}
